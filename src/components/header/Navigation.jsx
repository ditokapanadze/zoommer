import React from "react";
import { Icon } from "@chakra-ui/react";
import { HiMenu } from "react-icons/hi";

const Navigation = () => {
  return <Icon as={HiMenu} fill='dark' w={8} h={8} />;
};

export default Navigation;
